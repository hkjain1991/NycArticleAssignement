package com.example.nycarticlesassignment.enum

/**
 * Enum for showing the error for article list screen
 * @author hemeandra jain
 */
enum class ArticleScreenError {
    INTERNET_NOT_AVAILABLE,
    RESULTS_NOT_AVAILABLE
}